import { transitions, positions } from 'react-alert';

const options = {
  position: positions.BOTTOM_CENTER,
  timeout: 5000,
  offset: '30px',
  transition: transitions.SCALE,
};

export { options as alertOptions };
